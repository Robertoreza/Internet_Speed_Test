# ============================================================
#  PULSE/10 — Internet Speed Benchmark (Python GUI)
#  Requires Python 3.9+ · works on macOS / Linux / Windows
#
#    pip install -r requirements.txt
#    python speed_test.py
#
#  NOTE: tkinter ships with Python. If it is missing:
#    Linux  -> sudo apt install python3-tk
#    macOS  -> brew install python-tk   (Homebrew Python)
# ============================================================

# Measurement engine (Ookla speedtest.net bindings)
speedtest-cli>=2.1.3

# Graph: download/upload chart embedded in the GUI (pulls numpy)
matplotlib>=3.8.0

# ------------------------------------------------------------
# Optional, not required by speed_test.py:
#   customtkinter>=5.2.2     # themed widgets if you restyle it
# ------------------------------------------------------------
# This repo also ships a browser build of the same benchmark
# (see package.json) — that one is installed via npm, not pip.
